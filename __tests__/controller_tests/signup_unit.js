const mongoose = require("mongoose");

const controller = require("../../controllers/controller.js");

const User = require("../../models/schema.js");

describe("Unit test checking signUp from controller.js,", () => {});
